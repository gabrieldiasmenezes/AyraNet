@page
@{
    ViewData["Title"] = "Ajuda";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Ayra.Api</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"  rel="stylesheet">

    <!-- Font Awesome (Ícones) -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"  rel="stylesheet">

    <!-- CSS Personalizado -->
    <style>
        /* Estilo Geral */
        body {
            background-color: #e9ecef; /* Fundo cinza claro */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .container {
            margin-top: 30px;
            padding: 20px;
            border-radius: 10px;
            background-color: #ffffff; /* Fundo branco para o conteúdo */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Sombra suave */
        }

        h1 {
            font-size: 2.5rem;
            color: #0d6efd; /* Azul vibrante para o título */
            text-align: center;
            margin-bottom: 30px;
        }

        /* Estilo dos Cards */
        .card {
            border: none; /* Remove a borda padrão do card */
            border-radius: 10px; /* Bordas arredondadas */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Sombra suave */
            transition: transform 0.3s ease; /* Animação suave ao passar o mouse */
        }

        .card:hover {
            transform: scale(1.05); /* Aumenta ligeiramente o tamanho ao passar o mouse */
        }

        .card-body {
            padding: 15px; /* Espaçamento interno do card */
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .card-text strong {
            color: #343a40; /* Cor escura para os labels */
        }

        .icon {
            margin-right: 8px;
        }

        /* Cores Dinâmicas para os Cards */
        .bg-info-light {
            background-color: #e7f3ff !important; /* Azul claro */
            color: #0d6efd !important; /* Texto azul escuro para contraste */
        }

        .bg-warning-light {
            background-color: #fff3cd !important; /* Amarelo claro */
            color: #856404 !important; /* Texto marrom para contraste */
        }

        .bg-success-light {
            background-color: #d4edda !important; /* Verde claro */
            color: #155724 !important; /* Texto verde escuro para contraste */
        }

        /* Estilo do Botão de Ajuda */
        .help-btn {
            padding: 10px 20px; /* Espaçamento interno */
            font-size: 1rem; /* Tamanho da fonte */
            border-radius: 50px; /* Bordas arredondadas (estilo "pílula") */
            background-color: #0d6efd; /* Azul vibrante */
            color: white; /* Texto branco */
            border: none; /* Sem borda */
            transition: transform 0.3s ease, box-shadow 0.3s ease; /* Animação suave */
            text-decoration: none; /* Remove sublinhado do link */
            display: inline-flex; /* Alinha ícone e texto */
            align-items: center; /* Centraliza verticalmente */
        }

        .help-btn:hover {
            transform: scale(1.05); /* Aumenta ligeiramente ao passar o mouse */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Sombra ao passar o mouse */
            background-color: #0b5ed7; /* Azul mais escuro no hover */
        }

        .help-btn .icon {
            margin-right: 8px; /* Espaçamento entre o ícone e o texto */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4"><i class="fas fa-question-circle icon"></i> Página de Ajuda</h1>

        <div class="row">
            <div class="col-md-12">
                <div class="card bg-info-light mb-4">
                    <div class="card-body">
                        <h5 class="card-title"><i class="fas fa-info-circle icon"></i> Sobre o Sistema</h5>
                        <p class="card-text">
                            Este sistema foi desenvolvido para auxiliar em situações de emergência, fornecendo alertas climáticos e informações de segurança.
                        </p>
                    </div>
                </div>

                <div class="card bg-warning-light mb-4">
                    <div class="card-body">
                        <h5 class="card-title"><i class="fas fa-list-ul icon"></i> Funcionalidades Principais</h5>
                        <ul class="card-text">
                            <li>Lista de Alertas: Exibe todos os alertas cadastrados no sistema.</li>
                            <li>Filtro por Intensidade: Os alertas são destacados com cores diferentes (vermelho, amarelo, verde).</li>
                            <li>Detalhes dos Alertas: Cada alerta mostra informações detalhadas, como localização, raio e tempo de evacuação.</li>
                        </ul>
                    </div>
                </div>

                <div class="card bg-success-light mb-4">
                    <div class="card-body">
                        <h5 class="card-title"><i class="fas fa-code icon"></i> Endpoints da API</h5>
                        <ul class="card-text">
                            <li>GET /api/alerts: Retorna todos os alertas cadastrados.</li>
                            <li>POST /api/alerts: Cria um novo alerta.</li>
                            <li>PUT /api/alerts/{id}: Atualiza um alerta existente.</li>
                            <li>DELETE /api/alerts/{id}: Remove um alerta pelo ID.</li>
                        </ul>
                    </div>
                </div>

                <div class="card bg-light mb-4">
                    <div class="card-body">
                        <h5 class="card-title"><i class="fas fa-book icon"></i> Exemplo de Uso</h5>
                        <p class="card-text">
                            Para acessar a lista de alertas, navegue até <code>/Alerts/Index</code>. Para usar a API, utilize ferramentas como Postman ou Swagger.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (opcional, se precisar de funcionalidades interativas) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script> 
</body>
</html>